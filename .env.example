# Caminho, no computador host, para o Application Default Credentials (ADC).
# Gere o arquivo com: gcloud auth application-default login
GOOGLE_APPLICATION_CREDENTIALS_HOST=/caminho/absoluto/application_default_credentials.json
